name: fluttergallery
description: Flutter Gallery

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+13

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^0.1.3
  google_fonts: ^1.1.0
  universal_html: ^1.2.3
  http: ^0.12.1
  url_launcher: ^5.4.10
  photo_view: ^0.9.2

flutter:

  uses-material-design: true